class SSBConfig:
    """
    Manages S3 sync configuration for the folders.

    Internally constructs appropriate S3 Bucket connectors and passes them to individual folder handlers (SyncDir).
    """
    pass